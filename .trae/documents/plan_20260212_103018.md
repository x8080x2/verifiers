# Comprehensive Code Audit & Wiring Plan

I will thoroughly review the entire codebase (`app.py` and its dependencies) to ensure every UI element is correctly "wired" to the backend logic and that the application functions as a cohesive, production-ready unit.

## 1. Audit Phase (What I will check)
I will inspect `app.py` for:
- **UI/Backend Disconnects**:
  - Does the "Reset" button actually reset all state (charts, file inputs, text areas)?
  - Does the "Download Valid" button handle empty lists gracefully?
  - Are error states (network fail, API error) displayed to the user in the UI (not just console)?
  - Is the "Operational" status indicator real or hardcoded?
- **Logic Gaps**:
  - Does the file upload logic *really* fallback to the text area if no file is selected? (I recall implementing this, but I'll double-check the precedence logic).
  - Are we handling CSV files correctly (parsing header vs no-header)?
  - Is the progress bar calculation robust against division by zero?

## 2. Refinement Plan (How I will wire/fix it)
Based on the audit, I will update `app.py` to:
- **Wire the "Operational" Badge**: Add a simple startup check to the backend that pings the DeBounce API balance endpoint. If successful, the frontend will show "Operational"; otherwise, "API Error".
- **Fix "Download" Logic**: Ensure it only enables if there are > 0 valid emails.
- **Enhance CSV Parsing**: Use Python's `csv` module instead of simple string splitting to handle complex CSVs properly.
- **Robust Error Handling**: Add a `try-catch` block around the entire validation loop to catch backend crashes and send a "fatal_error" event to the frontend, which will display a toast notification.
- **Cleanup**: Remove any unused CSS classes or JS variables from previous iterations.

## 3. Documentation
I will generate a `ARCHITECTURE.md` file documenting:
- **Data Flow**: `User Input -> Flask Route -> Generator Stream -> SSE -> JS EventSource -> DOM Update`.
- **Key Functions**: Explaining `startValidation()`, `updateProgress()`, and the Python generator logic.
- **State Management**: How the JS frontend maintains the `stats` object.

## 4. Execution
- Stop server.
- Apply fixes/wiring to `app.py`.
- Create `ARCHITECTURE.md`.
- Restart server.

# Bulk Email Validation: Step 1 (Upload)

I will start by creating the test file and the upload script, using the provided API key and the specific email you requested.

## Step 1: Upload Emails (`step1_bulk_upload.py`)
1.  **Create Test Data (`emails.txt`)**: I will create a file containing:
    *   `jaco@smei.co.za` (Valid, as requested)
    *   `invalid-email-format` (Invalid format)
    *   `disposable@mailinator.com` (Likely disposable/risky)
2.  **Script Logic**:
    *   Read `emails.txt`.
    *   Upload to `https://api.debounce.io/v1/bulk/upload` using your API key (`698d96071ccf3`).
    *   Print the JSON response, which will give us the `job_id` needed for the next steps.

## Future Steps (After Upload Confirmation)
*   **Step 2**: Check the status of the job until it's finished.
*   **Step 3**: Download the results and **filter to show only "Deliverable" emails**, as requested.
